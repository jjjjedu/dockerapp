machine:
  pre:
    - curl -sSL https://s3.amazonaws.com/circle-downloads/install-circleci-docker.sh | bash -s -- 1.10.0
  services:
    - docker

dependencies:
  pre:
    - sudo pip install docker-compose==1.9.0 

test:
  override:
    - docker-compose up -d
    - docker-compose run dockerapp python test.py


deployment:
  hub:
    branch: /.*/
    commands:
      - docker login -u jjjjedu -e jevgeni172@gmail.com -p testpass 
      - docker tag dockerapp_dockerapp jjjjedu/dockerapp:$CIRCLE_SHA1
      - docker tag dockerapp_dockerapp jjjjedu/dockerapp:latest
      - docker push jjjjedu/dockerapp:$CIRCLE_SHA1
      - docker push jjjjedu/dockerapp:latest

